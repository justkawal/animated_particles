name: animated_particles
description: "Fast, smooth steering-behavior particle field - particles form shapes/text from sampled atoms, morph, and flee touch."
version: 0.1.0
homepage: https://github.com/justkawal/animated_particles

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0

flutter:
